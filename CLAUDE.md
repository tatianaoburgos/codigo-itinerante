# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## O que é este projeto

Sites institucionais para hostels/pousadas oferecidos em permuta por noites de hospedagem. Arquitetura "build once, reuse everywhere": um motor Astro reutilizável em `template/`, dados por cliente em `clientes/<hostel>/` (config.json + fotos). Monorepo privado; cada cliente vira um projeto separado na Vercel apontando para este repo, com variável de ambiente selecionando qual cliente buildar. Desde 2026-07-31 em exploração inicial de um segundo vertical fora de hostels — projetos sociais/ONGs (Projeto Viva, ver "Estado atual") —, ainda sem cliente em `clientes/` nem decisão sobre se o motor `template/` será reaproveitado ou se o vertical terá arquitetura própria.

**Diretrizes de design obrigatórias em qualquer trabalho visual**: `docs/diretrizes-design.md` (o que todo site SEMPRE tem e o que NUNCA tem — ex.: hero full-bleed, rolagem só vertical, sem vídeos/carrosséis/popups/emojis) e `docs/preferencias-visuais.md` (catálogo de referências visuais por site analisado, para propor opções com exemplo que a autora consiga ver — específico do vertical hostel; projetos sociais têm catálogo próprio em `docs/preferencias-visuais-projetos-sociais.md`). Para iniciar site de cliente novo, usar a skill `/novo-projeto` (`.claude/skills/novo-projeto/SKILL.md`): pesquisa Maps/Instagram/Booking primeiro, confirma o que a pesquisa já respondeu, pergunta o resto uma pergunta por vez com opções + recomendação + referência visual.

Contexto de negócio e decisões fechadas estão em `docs/projeto.md` e `docs/decisoes-tecnicas.md` — leia antes de propor mudanças de arquitetura; as decisões lá registradas (Astro + Tailwind, config.json sem CMS, monorepo, componentes compartilhados) já foram deliberadas e não devem ser reabertas sem motivo forte. A identidade visual da própria marca está em `docs/miv.md` (Manual de Identidade Visual) e o questionário reutilizável de onboarding de cliente em `docs/briefing-cliente.md`.

## Comandos

Há dois projetos Astro independentes, cada um com seu próprio `package.json` e `node_modules`:

**`template/`** — o motor dos sites de cliente (buildado por cliente via env `CLIENTE`):

```
cd template
npm install
CLIENTE=lumehostel npm run dev       # servidor de desenvolvimento (padrão: demo)
CLIENTE=lumehostel npm run build     # build estática
npm run preview                      # serve a build localmente
npm run check                        # astro check (tipos, sem lint dedicado)
```

No PowerShell (Windows), a sintaxe `VAR=x cmd` não funciona — defina a env antes: `$env:CLIENTE = 'lumehostel'; npm run dev`.

O servidor de dev (`astro dev`) não observa mudanças em `clientes/<hostel>/config.json` (fica fora da raiz do Vite, em `template/`) — depois de editar o config.json do cliente ativo é preciso reiniciar o `npm run dev` para a mudança ser recarregada; arquivos dentro de `template/src/` recarregam sozinhos (HMR normal).

**`site/`** — o site da própria marca (landing page única, projeto separado):

```
cd site
npm install
npm run dev
npm run build
npm run og        # regenera a imagem Open Graph via sharp
```

Não há testes nem linter configurados ainda.

**Deploy de um cliente novo no Vercel** (receita usada para LumeHostel e Mar à Vista, via `vercel` CLI): cada cliente é um projeto Vercel **separado**, mas todos com **Root Directory = `.`** (raiz do monorepo, não `template/`) — assim o build enxerga `../clientes/<slug>` a partir de `template/astro.config.mjs`. O `template/` sozinho não basta como raiz do deploy. Comandos do projeto (setados via `vercel project update <nome> --framework astro --build-command "cd template && npm run build" --install-command "cd template && npm install" --output-directory "template/dist"`) fazem o `cd` para dentro de `template/`. Env var `CLIENTE=<slug>` setada nos 3 ambientes (`vercel env add CLIENTE production/preview/development --value <slug> --yes --cwd template`, rodado com o projeto novo já linkado). Projetos existentes (`lumehostel`, `codigo-itinerante`/site) ficam registrados em `.vercel/repo.json` (não versionado) com `directory: "template"` ou `"site"` — adicionar o cliente novo lá também para que `vercel deploy --project <nome> --cwd template` resolva certo.

## Arquitetura

- **Stack**: Astro 5 + Tailwind CSS 4 (via `@tailwindcss/vite`), site 100% estático, deploy Vercel free tier. Tailwind é importado em `src/styles/global.css`.
- **Dados por cliente**: cada cliente tem `clientes/<hostel>/config.json`, validado por schema Zod (`template/src/lib/schema.ts`) na build — a build falha com dados inválidos. O cliente ativo é escolhido pela variável de ambiente `CLIENTE` (padrão `demo`) em `template/src/lib/cliente.ts`. `clientes/demo/` é o hostel fictício de vitrine ("Hostel Maré Alta"). Campo `precos` removido do schema em 2026-07-23 (nunca era renderizado — preço nunca aparece no site, política é sempre "consultar no WhatsApp"; não confundir com o `precos.faixa` que já não existia).
- **Formato do site**: **one-page com âncoras** (desde 2026-07-13, substituiu a variação multipágina). Uma única rota `/` compõe, nesta ordem: hero em tela cheia → acomodações em grade (cards boutique, 2/3 colunas) → comodidades ilustradas → a região → depoimentos → localização → CTA final. O menu fica atrás de um ícone de hambúrguer (ver padrão de Nav abaixo). Seções cujo campo não existe no config (`regiao`, `depoimentos`) simplesmente não renderizam, e o Nav esconde a aba correspondente.
- **Componentização**: biblioteca única de componentes compartilhados (hero, galeria, grade de acomodações, mapa, CTA WhatsApp, nav/footer, divisor de símbolo, marca-d'água). ~80% do código compartilhado entre clientes; cada cliente = mesma composição + tema visual próprio.
- **Padrão de Nav (desde 2026-07-23)**: logo sempre colada à esquerda; à direita, sempre na mesma linha (nunca quebra, em nenhuma largura), o `BotaoWhatsApp` (texto completo sempre visível, em qualquer largura — decisão deliberada; a variante ícone-só que existiu em algum momento foi removida do componente) e, colado na borda, um ícone de hambúrguer. O menu (links + itens inertes de `navFuturo`) abre num painel via **Popover API nativa** (`popover` attribute no `<div id="menu-nav">`, `popovertarget` no botão) — dismiss automático ao clicar fora ou Esc, sem JS; um pequeno script sincroniza `aria-expanded`/`aria-label` do botão com o evento `toggle` do popover (leitor de tela anuncia "abrir"/"fechar" corretamente). A animação do ícone (3 traços → X) é só Tailwind, via `group-has-[#menu-nav:popover-open]:...` no `header` (com classe `group`). **Pegadinha real já resolvida**: qualquer classe que force `display` incondicional (ex.: `flex` solto) num elemento `[popover]` anula o `display:none` nativo do estado fechado — CSS de autor sempre vence UA, independente de especificidade — por isso o `display:flex` só é aplicado condicionalmente via `[&:popover-open]:flex`. Da mesma forma, sobrescrever só `top`/`right` sem também `left-auto` deixa o `left:0` do `inset:0` padrão do popover ainda ativo, o que sobre-restringe a caixa e (em LTR) o navegador ignora o `right` — sempre setar `left-auto` junto. Campo `logoFixoEsquerda` (do layout antigo) foi removido do schema — o layout novo é único, sem variação por cliente.
- **Tema por cliente**: cada cliente tem seu tema em `template/src/styles/temas/<slug>.css` (bloco `@theme` com tokens de cor + `--font-*`, e os `@import` das fontes daquele cliente). O `global.css` só faz `@import "@tema"` (antes do `@import "tailwindcss"`) + a base; o alias `@tema` é resolvido em `template/astro.config.mjs` a partir da env `CLIENTE` (arquivo ausente derruba a build). Os nomes de token são compartilhados (`mare`, `noite`, `azulejo`, `sal`, `espuma`, `fitinha`, `zap`) — funcionam como slots reaproveitados entre clientes; renomear para papéis neutros é follow-up. Fontes variáveis auto-hospedadas via `@fontsource-variable/*`.
- **Imagens**: fotos versionadas na pasta do cliente; otimização na build via `astro:assets` (converte para WebP automaticamente, mesmo sem `format=` explícito nos componentes `<Image>` — confirmado inspecionando o `dist/` gerado, não é preciso setar o formato manualmente); alt text vem do config.json. As fotos ficam fora da raiz do Vite (`template/`), liberadas por `vite.server.fs.allow` em `template/astro.config.mjs` — restrito a `../clientes` e `../marca` (não o monorepo inteiro) desde 2026-07-23.
- **Mosaico de fotos** (grade alternando foto grande/pequena, usada em "A região" e "A vibe"): a lógica de aspect-ratio cíclico por índice é compartilhada via `template/src/lib/mosaico.ts` (`aspectMosaico`) desde 2026-07-23 — o resto da composição (padrão de col-span, legenda, hover) ficou deliberadamente separado em cada uso, por serem diferenças reais de design, não duplicação acidental.
- **Vídeo do hero** (`marca.heroVideo`, opcional): não passa por `astro:assets` (não otimiza vídeo) nem por `import.meta.glob` (exigiria padrão estático `clientes/*/fotos/*.mp4`, que casaria e emitiria no dist o vídeo de **todos** os clientes, não só do ativo — vazamento real já corrigido). Em vez disso, um plugin Vite dedicado em `template/astro.config.mjs` (`servir-video-cliente`) lê `marca.heroVideo` direto do `config.json` do cliente ativo e serve/emite só esse arquivo exato.
- **Marca do negócio (`marca/`)**: ativos da identidade visual da própria marca **Código Itinerante** (o negócio que revende os sites), distinta dos temas por cliente em `template/src/styles/temas/` — não confundir. Identidade "Estrada 1 — Lâmpada de hostel"; manual completo em `docs/miv.md`. Contém wordmark e monograma em SVG nas 3 variantes (`-escuro`/`-claro`/`-mono`, com as fontes convertidas em curvas — não dependem de fonte instalada), `tokens.css` (custom properties `--ci-*`) e o gerador `gera_wordmark.py`. Para regenerar os SVGs, as instruções (instalar `fonttools`, baixar Archivo + Instrument Serif Italic, rodar o script) estão na docstring de `marca/gera_wordmark.py`; os `.ttf` ficam ao lado e não são versionados. Versão navegável do manual em `marca/miv.html` (gerada a partir de `docs/miv.md`), com exportação em PDF em `marca/MIV-codigo-itinerante.pdf`. Template Word em `marca/template-documento.docx` (Fase 2, 2026-07-20): gerado por `marca/gera_template_word.py`, aplica o cabeçalho/rodapé/tipografia de `docs/miv.md` §5 com conteúdo placeholder; `marca/prepara_fontes_documento.py` instala as fontes "Archivo Doc"/"Martian Mono Doc" (faces estáticas derivadas das variáveis, nomes próprios para não colidir com uma instalação futura das fontes completas) por usuário no Windows e roda uma vez por máquina.
- **Site institucional, não motor de reservas**: 4 abas (Acomodações, Sobre, Localização, Contato/WhatsApp). Sem checagem de disponibilidade, sem pagamento, sem integração com OTAs. Evitar a palavra isolada "Reservas" no menu. Evitar preço exato no conteúdo — preferir faixa de preços ou "consulte no WhatsApp".

## Convenções

- Idioma do projeto (docs, conteúdo, commits): português brasileiro. Sites gerados usam `lang="pt-BR"`.
- Não copiar código, textos ou imagens do site de referência (nomadssalvador.com) — apenas estrutura.
- Ao incorporar templates de terceiros, verificar a licença: preferir MIT/Apache 2.0, evitar GPL/copyleft.
- **Todo documento comercial real (contrato, proposta, briefing) entregue a um cliente usa sempre o sistema visual do template Word** (`marca/gera_template_word.py`, `docs/miv.md` §5) — nunca texto puro. O conteúdo de cada documento nasce como fonte em Markdown (ex. `docs/contrato-permuta.md`, modelo genérico com placeholders); o `.docx` final de cada cliente é gerado **sob demanda**, a partir do questionário de intake preenchido (`docs/gera_questionario_contrato.py`) — não existe (nem está previsto) um gerador automático que rode sem um cliente real.

## Estado atual

Motor completo como **one-page** (`/` apenas), JSON-LD LodgingBusiness no layout. Spec e plano do redesign: `docs/superpowers/specs/2026-07-13-lumehostel-onepage-design.md` e `docs/superpowers/plans/2026-07-13-lumehostel-onepage.md`. Convenção: todo cliente precisa de `fotos/capa.jpg` (imagem do hero, decorativa) **ou**, alternativamente, de `marca.heroVideo`/`heroVideoPoster` (hero em vídeo, ver Mar à Vista abaixo) — `template/src/lib/fotos.ts` derruba a build se um arquivo referenciado no config não existir. Campos do config: `comodidades` (nome + descricao/foto/simbolo opcionais; `simbolo` referencia a biblioteca de SVGs artísticos em `SimboloComodidade.astro`), `regiao`, `depoimentos`, `vibe` (mosaico de fotos livres, componente `Vibe.astro`) e `destaques` (opcionais; `destaques` = até 2 seções full-bleed com foto fixa ao rolar + frase, técnica clip-path), `marca.simbolo` (divisor de seções e marca-d'água). Movimento sem vídeo: classe `.revela` (surgimento suave via IntersectionObserver no Layout, respeita `prefers-reduced-motion`).

**Upgrade do Astro para v7 concluído e em produção** (2026-07-24 a 2026-07-28; branch `deps/astro-upgrade-v7`, mesclada em `master` por fast-forward em `139f635` após aprovação explícita da autora): motivado por 3 vulnerabilidades de dependência (astro, esbuild, sharp aninhado) que sobraram do `npm audit fix` de 2026-07-23, resolvidas só a partir do Astro 7.1.3. Plano completo em `docs/superpowers/plans/2026-07-23-astro-upgrade-v7.md`, executado via `superpowers:subagent-driven-development` (ledger em `.superpowers/sdd/progress.md`). `template/`: astro `5.10.0`→`7.1.3`, vite unificado em `8.1.5` (override de `@tailwindcss/vite` ajustado de `^6.4.3` para `^8.0.0`), `engines.node: ">=22.12.0"` adicionado ao `package.json` (piso que o próprio Astro 7 passou a exigir internamente). `site/`: mesmo upgrade (astro `7.1.3`, `@astrojs/check` `0.9.9`, override de vite `^8.0.0`, `engines.node` `>=22.12.0`); `npm audit` zerou sem precisar de fix extra (diferente do `template/`, que precisou de um `npm audit fix` adicional pro `sharp` aninhado). `check`/`build` limpos nos 3 clientes + `site/`, smoke visual confirmado nos dois (LumeHostel via browser real; `site/` via `scrollTo` em pixels exatos, confirmando o degradê breu→âmbar contínuo sem corte ao longo dos 5031px da página). Astro 7 trocou `astro dev` por um modelo de daemon persistente (`astro dev stop/status/logs`, não mais `Ctrl+C`) — usar `npx astro dev stop` para derrubar o servidor. **Achado na hora do deploy**: só o projeto `codigo-itinerante` (site da marca) é Git-conectado na Vercel e redeploya sozinho a partir de push em `master` (alias `-git-master-` confirma); `lumehostel` e `maravista` **não são** Git-conectados — deploy de produção é sempre manual via `vercel deploy --project <nome> --cwd template --prod --yes`, rodado a partir da raiz do monorepo. Os 3 sites confirmados no ar com o Astro v7 (HTTP 200 em `lumehostel.vercel.app`, `maravista-plum.vercel.app`, `www.codigoitinerante.com.br`).

**Ícones de redes sociais no rodapé concluído** (2026-07-31; commits `6330f7b`, `713c212`; plano `docs/superpowers/plans/2026-07-31-redes-sociais-rodape.md`): campos opcionais `contato.tiktok`/`contato.facebook` no schema; biblioteca nova `IconeRedeSocial.astro` (ícones desenhados à mão, `viewBox 0 0 48 48`, `stroke-width="4"`, `currentColor` — nunca ícones literais de terceiros, `docs/diretrizes-design.md`) com Instagram/TikTok/Facebook. `Footer.astro` ganhou o bloco "Siga a gente", condicional a pelo menos uma rede preenchida, expandindo o grid de 2 para 3 colunas quando presente — muda o rodapé de `demo` e `maravista` também (Instagram, que já existia nos dois, passa de link de texto pra ícone). LumeHostel ganhou TikTok (`@lumehostel`); Facebook segue sem uso em nenhum cliente até agora. `check`/build dos 3 clientes confirmados limpos, check visual do rodapé do LumeHostel feito no navegador.

Primeiro cliente real em andamento: **LumeHostel** (`clientes/lumehostel/`), com tema terracota/âmbar do MIV aplicado (`template/src/styles/temas/lumehostel.css` — ver `clientes/lumehostel/marca.md`), logo real extraído do PDF (`clientes/lumehostel/marca/`), favicon completo em terracota e conteúdo baseado **exclusivamente** nas presenças online do hostel (Booking/Maps/Instagram) — nunca inventar informação sobre o hostel. Fotos do hostel vêm das galerias públicas do próprio estabelecimento e as da região são de licença livre; proveniência de cada arquivo em `clientes/lumehostel/fotos/FONTES.md`; uso já autorizado pelo Gabriel (2026-07-23). Coworking e redário já têm foto própria; churrasqueira e sala de jogos ainda não (o material simplesmente não existe ainda, não é pendência de código). Tipografia: Space Grotesk em todo o site (a Arial Rounded MT Bold do MIV é proprietária — Gabriel tem a opção de comprar a licença web de qualquer uma das duas fontes do MIV; `marca.md` documenta a decisão). Fundo das seções usa o componente `CascataOndas.astro` (cascata tom sobre tom por seção) para evitar cor chapada; mosaicos de "A região" e "A vibe" alinhados em fileiras alternando foto grande/pequena. Publicado em produção na Vercel em `https://lumehostel.vercel.app` (2026-07-15; redeploy em 2026-07-23 com o Nav novo — ver padrão de Nav acima — e a hero sem o botão duplicado, que já estava corrigido no código desde a revisão de 2026-07-22 mas nunca tinha ido a produção; redeploy em 2026-08-11 com o site bilíngue pt/en — ver "Site bilíngue pt/en do LumeHostel implementado e verificado" acima — `/en/` confirmado no ar); falta domínio próprio. Pendências do Gabriel em `clientes/lumehostel/PENDENCIAS.md` — inclui decidir se mantém a seção de depoimentos (custo de manutenção). As 10 melhores avaliações públicas do hostel (Google Maps 5,0/52 e Booking 9,2/444) estão em `clientes/lumehostel/avaliacoes.md`; em 2026-07-14 o `config.json` já foi atualizado com depoimentos trocados a partir dessas avaliações do Google. **Auditoria dos 3 sites em 2026-07-23** (revisão geral + plano de execução): nomes dos depoimentos abreviados para primeiro nome + inicial (ex.: "Gabriel B.", padrão a seguir em clientes futuros); `Nav.astro` (motor) ganhou item de menu para `#depoimentos`, que antes só existia rolando a página. Build/preview: `CLIENTE=lumehostel`.

**Site bilíngue pt/en do LumeHostel implementado e verificado (2026-08-01 a 2026-08-11)**: `docs/superpowers/plans/2026-07-31-lumehostel-i18n.md` (spec em `docs/superpowers/specs/2026-07-31-lumehostel-i18n-design.md`), todas as 12 tasks completas e commitadas em `master` (`f65d403`..`add339a`, mais a verificação final em 2026-08-11). Motor ganhou roteamento i18n nativo do Astro (`/en/`, guardado por `idiomasDisponiveis` no config — `demo` e `maravista` continuam redirecionando `/en/` para `/` e sem o alternador, sem mudança de comportamento), campo bilíngue no schema (`campoLocalizavel`, união `string | {pt, en}`), dicionário de textos fixos (`lib/textos.ts`) e helpers `t()`/`idiomaAtual()` (`lib/i18n.ts`). Só o LumeHostel está ativado; conteúdo inteiro (slogan, acomodações, comodidades, região, depoimentos, localização) traduzido no `config.json`. **Bug real corrigido durante a revisão do plano antes da execução**: o `Nav.astro` original deixava o link do logo e os itens do menu de seção fixos em `/`, o que levaria quem estivesse em `/en/` de volta pra página em português ao clicar — corrigido com uma variável `linkBase` derivada do idioma atual. **Verificação final (2026-08-11)**: builds limpas dos 3 clientes, `npm run check` sem erros, checklist manual de 10 itens no navegador (alternador PT/EN, navegação de menu/logo permanece em `/en/`, nota "Translated from Portuguese." nos depoimentos, WhatsApp com mensagem em inglês, rodapé "Follow us", `lang`/`hreflang`/`og:locale` corretos nos dois idiomas) — todos passaram, nenhum ajuste necessário. Branch/worktree `i18n/lumehostel-pt-en` (100% contida em `master`) removida por redundância.

**Espanhol no LumeHostel implementado, verificado e mesclado em master (2026-08-11 a 2026-08-14)**: motor generalizado de bilíngue (pt/en) para multilíngue (N idiomas), com espanhol ativado no LumeHostel. Spec em `docs/superpowers/specs/2026-08-11-lumehostel-espanhol-design.md`, plano de 9 tasks em `docs/superpowers/plans/2026-08-11-lumehostel-espanhol.md`, todas completas; branch `i18n/lumehostel-es` mesclada em `master` por fast-forward em 2026-08-14 e removida (worktree e branch). `campoLocalizavel` vira `string | {pt: obrigatório, en?: opcional, es?: opcional}`, com fallback pro `pt` em `t()` (`lib/i18n.ts`) quando o campo não tem a chave do idioma pedido; alternador do Nav e as tags `hreflang`/`og:locale`/`html lang` do Layout iteram sobre `idiomas` (`schema.ts`, ordem fixa pt→en→es) filtrado por `config.idiomasDisponiveis`. `og:locale` do espanhol usa `es_ES` (mesma lógica do `en_US`). A nota "Traduzido do português" nos depoimentos reconhece qualquer idioma não-pt (`idioma !== 'pt'`), não só inglês. Conteúdo inteiro do LumeHostel (slogan, acomodações, comodidades, região, depoimentos, localização, destaques) traduzido no `config.json`; `demo` e `maravista` seguem sem `idiomasDisponiveis`, redirecionando `/es/` para `/` sem alternador (confirmado por build real dos 3 clientes). Verificação: `npm run check` limpo, builds dos 3 clientes, checklist manual no navegador (alternador PT/EN/ES, navegação de menu/logo permanece no idioma atual, nota de tradução, WhatsApp em espanhol, rodapé "Contáctanos"/"Síguenos", `lang`/`hreflang`/`og:locale` corretos) — tudo reconfirmado com evidência fresca em 2026-08-14 antes do merge, não só pelos commits herdados da branch. Mistura de registro regional encontrada na revisão pré-merge (vocabulário de Espanha como "Nevera" ao lado de termos latino-americanos como "casillero"/"parrilla") corrigida antes do merge (`Nevera` → `Refrigerador`, único outlier em todo o `config.json`). Tradução ainda pendente de revisão da autora antes do deploy (mesmo método do inglês — Claude traduz, sem revisor nativo de espanhol). Deploy de produção é manual (`vercel deploy --project lumehostel --cwd template --prod --yes`) e push de `master` pra `origin`, ambos fora do escopo deste ciclo — `master` local ficou à frente do remoto.

**Projeto DESCONTINUADO (2026-08-11): o dono não aceitou a proposta.** Histórico preservado abaixo, sem trabalho ativo nem candidatura a próximo case de portfólio a menos que a autora reabra o assunto. Segundo cliente em POC pré-contato: **Mar à Vista Hostel** (`clientes/maravista/`, hostel na Barra/Salvador, branch e worktree `maravista-poc`), tema azul-marinho + azul vivo do logo com acento âmbar queimado (`template/src/styles/temas/maravista.css`). Em 2026-07-21, com o motor/marca/tema já prontos e as fotos em processamento, a autora pivotou de "site completo pré-contato" para uma **POC enxuta**: chegar ao dono com o site inteiro correria o risco de passar a impressão de estar vendendo algo grande demais antes de validar a ideia com ele. A POC entrega só hero full-bleed em vídeo (autoplay mudo, sem `loop` — congela no último frame — e respeita `prefers-reduced-motion` caindo para o poster estático) + CTA final + rodapé; o botão "Reserve pelo WhatsApp" é a única ação real do site. Nav mostra a arquitetura futura: os 5 itens ("O Hostel", "Acomodações", "Comodidades", "A região", "Localização") são todos rótulos inertes via `navFuturo`, sem simular navegação que não leva a lugar nenhum. **Atualização de 2026-07-23**: a seção "O Hostel" (`#sobre`, foto do lounge + texto com a nota do Booking) existiu brevemente mas foi removida no mesmo dia — decisão deliberada de minimalismo, não regressão (`config.sobre` virou opcional no schema, mesmo padrão de `acomodacoes`/`comodidades`; página pula direto do hero pro CTA). "O Hostel" também virou rótulo inerte, então hoje **nenhum** item do menu é âncora real. O e-mail de contato no `config.json` (`contato@maravistahostel.example.com`) é um placeholder proposital — o site nesta fase é uma amostra pré-contrato para mostrar ao dono, não a entrega final; trocar pelo e-mail real quando o acordo fechar. Spec e plano do pivô: `docs/superpowers/specs/2026-07-21-maravista-poc-pivot-design.md` e `docs/superpowers/plans/2026-07-21-maravista-poc-pivot.md`. **O plano de site completo (`docs/superpowers/plans/2026-07-20-maravista-poc.md`) não foi descartado — fica preservado e pausado**, para retomar quando a autora decidir mostrar o site cheio ao dono. Mudanças no motor viabilizadas pelo pivô, reutilizáveis por clientes futuros: `Hero.astro` ganhou suporte opcional a vídeo (campo `marca.heroVideo` + `heroVideoPoster`, mesmo princípio de fallback já usado em `regiao`/`depoimentos`); `acomodacoes` e `comodidades` viraram opcionais no schema (antes obrigatórios via `.min(1)`), permitindo montar uma página só com hero + uma seção. O vídeo do hero (drone da orla da Barra, extraído do Instagram, ~23s processados) já tem autorização de uso do dono do hostel (confirmado em 2026-07-23). **Publicado em produção na Vercel em `https://maravista-plum.vercel.app` (2026-07-23)**, projeto `maravista` configurado com o mesmo padrão do LumeHostel (Root Directory `.`, Build Command `cd template && npm run build`, env `CLIENTE=maravista`); falta domínio próprio. Redeploy no mesmo dia com o Nav novo (padrão de hambúrguer, ver seção de Arquitetura) — nessa primeira versão do Nav novo os 5 itens ainda eram 1 âncora real ("O Hostel") + 4 `navFuturo` inertes; a remoção da seção "O Hostel" (ver acima) tornou os 5 itens todos inertes. Build/preview local: `CLIENTE=maravista`.

**Novo vertical em exploração inicial (2026-07-31): Projeto Viva.** Primeiro prospect fora do vertical hostel — projeto social, ainda em fase de pesquisa de referências visuais, sem `clientes/viva/` criado e sem decisão sobre se o motor `template/` será reaproveitado ou se o vertical terá arquitetura própria. Tom desejado: alegre, colorido, cheio de vida — o oposto deliberado do tom de culpa/doação do site amigosdobem.org (analisado como contraexemplo, não como "errado" em absoluto: esse tom pode caber em outro projeto social no futuro). Catálogo de referências visuais analisadas (Amigos do Bem, Casa do Zezinho, Instituto Beja, Doutores da Alegria, Instituto Ayrton Senna, FADC) em `docs/preferencias-visuais-projetos-sociais.md`. Perguntas em aberto para a cliente, registradas no mesmo doc: qual o objetivo do site (informar, captar doação, voluntariado, parcerias, ou canal de contato — lista completa no doc), se há CTA fixo de WhatsApp, interesse na arquitetura de foto fixa no background tipo "Doe agora" do Amigos do Bem, se ela tem vídeos para um hero em vídeo (referência: casadozezinho.org.br), se quer a seção de valores/ODS mais dinâmica, e se o projeto tem parcerias a destacar.

**Projeto EM PAUSA (2026-08-11): o cliente não conseguiu passar as senhas de acesso e não quer site novo.** Pode retomar se a situação mudar; sem próximos passos ativos (contrato, .docx final) até lá. **Prospecção CasaMarelo iniciada (2026-08-02/03)**: primeiro contato considerado com o CasaMarelo (`casamarelo.com`, hostel na Barra/Salvador — mesmo bairro do Mar à Vista, já anotado como referência visual em `docs/preferencias-visuais.md` desde 2026-07-31, "site de hostel simples e bonitinho... gostinho de casa"). Auditoria feita clicando em cada link do site real, não só pelos nomes do menu: o seletor de idioma "BR | ES | EN" é decorativo (ES/EN com `href="#"`, e `/en/`/`/es/` retornam 404 renderizando o conteúdo em português por baixo — nenhuma palavra traduzida existe em lugar nenhum); os itens de menu "Avaliações" e "CasAMARelo Cultural" abrem seções vazias, só o título; os 5 ícones de rede social (2 no topo, 3 no rodapé) são todos `href="#"`; sem dado estruturado (JSON-LD), meta description, canonical ou hreflang no `<head>`. Site roda em WordPress com tema feito sob encomenda (`wp-content/themes/casamarelo-site/`, sem page builder), hospedado em Apache/cPanel — stack incompatível com o motor Astro/Vercel da Código Itinerante; autora cogitou editar o site atual deles em vez de reconstruir do zero, ainda sem resposta sobre o motivo (razões técnicas contra já levantadas: acesso precisaria ser cPanel/FTP/WordPress em vez de Perfil da Empresa no Google, zero reaproveitamento com o motor `template/` compartilhado entre clientes, hospedagem continuaria sendo deles em vez do Vercel da autora). **Decisão fechada**: escopo padrão de sempre — reserva só por WhatsApp, sem integrar o motor de reservas real que eles usam hoje ("Foco"/`motor-reserva.com.br`); cláusula 2 do `docs/contrato-permuta.md` já cobre essa exclusão. **Escopo novo, inédito até aqui**: entrega bilíngue de verdade em três idiomas — português, inglês e espanhol (primeira vez com espanhol; o inglês do LumeHostel também segue só como plano não implementado, ver acima). Diária real informada pela autora: R$70 — pela fórmula padrão (R$3.000 de referência ÷ diária, piso de 20 noites, ver `docs/contrato-permuta.md`), dá ~42,86 noites; arredondamento (42 ou 43) e eventual ajuste do valor de referência pelo escopo trilíngue ainda em aberto. Dois materiais de proposta já gerados para mostrar à cliente — sem número de noites nem o valor de referência interno, que ficam reservados para o contrato, redigido depois: `docs/proposta-casamarelo.docx` (gerado por `docs/gera_proposta_casamarelo.py`, reaproveitando o sistema visual de `marca/gera_template_word.py`) e `mockups/proposta-casamarelo-visual.html` (one-pager visual, mesma paleta/tipografia da marca, pensado pra convencer rápido).

**Revisão da hero em 2026-07-22** (limpeza + composição centralizada): motor compartilhado ganhou dois ajustes que valem para todos os clientes — `Hero.astro` não mostra mais o botão "Reserve pelo WhatsApp" duplicado dentro da hero (fica só o do menu fixo) e o vídeo passa a tocar duas vezes (listener `ended` com `{ once: true }`) antes de congelar no último frame; `Nav.astro` parou de esmaecer (`opacity-40`) os rótulos inertes do `navFuturo`, que agora têm a mesma cor do link real. Três campos novos e opcionais em `marcaSchema`, usados só pelo Mar à Vista por enquanto: `logoFixoEsquerda` (logo do menu não centraliza no layout mobile empilhado; os itens do menu passam a seguir logo à direita do logo, sem o vão do `justify-between`), `logoApenasNoMenu` (a hero usa o nome em texto em vez do selo circular do logo, que não escala bem em tamanho grande) e `heroCentralizado` (troca o bloco clássico ancorado embaixo à esquerda por uma composição nova: nome em duas linhas grande e centralizado na tela — "Mar à Vista" / "Hostel", via `max-width` em `ch` — e, no canto inferior direito, a frase em negrito âmbar também em duas linhas ("Mar à vista" / "em todos os quartos", quebra manual via novo campo `heroFraseQuebrada` — fallback pro `slogan` normal, que continua limpo pro title/footer) com o símbolo `icone.png` em cor cheia logo abaixo, reduzido e colado na base da tela (`bottom-0`); inspirada em referências de hospedagem boutique como surfcamparara.com e uxua.com. Frase da hero em âmbar queimado (`--color-fitinha`) via override em `maravista.css` (`#hero .hero-frase`), não no componente compartilhado. Tipografia de destaque avaliada por mockup com toggle (`mockups/maravista-hero-refino.html`): manteve Bricolage Grotesque por ora, mas Big Shoulders Display agradou bastante — oferecer de novo se a autora reabrir essa discussão. Commits: `c2b9184`, `fc12e9c`, `b2f9f22`.

Questionário de briefing para clientes (reutilizável): `docs/briefing-cliente.md`. Pergunta identidade visual primeiro (MIV ou fallback logo/cores/estilo); preço é política fixa comunicada como combinado ("consultar no WhatsApp", sem faixa/valor no site) — nunca pergunta ao cliente.

**Site da própria marca construído em `site/`** (2026-07-03): landing page única (spec em `docs/superpowers/specs/2026-07-03-site-codigo-itinerante-design.md`), projeto Astro separado do motor — comandos `npm run dev/build/preview` dentro de `site/`, mais `npm run og` (regenera a imagem Open Graph via sharp). Textos aprovados pela autora seção por seção; dados reais preenchidos e site publicado na Vercel (`https://codigo-itinerante.vercel.app`, definido em `site/astro.config.mjs`). SEO básico (sitemap.xml + robots.txt via `@astrojs/sitemap`, mesmo padrão do `template/`) adicionado em 2026-07-20. **Seção de portfólio concluída em 2026-07-22** (`ProvaDeTrabalho.astro`, entre "Quem sou eu" e "Como funciona"): case único mostrando o LumeHostel (`https://lumehostel.vercel.app`) como prova de trabalho real, com moldura de navegador em markup/Tailwind + capturas reais desktop/mobile — sem depoimento do Gabriel por enquanto (fica para depois). Spec e plano: `docs/superpowers/specs/2026-07-20-site-prova-de-trabalho-design.md` e `docs/superpowers/plans/2026-07-20-site-prova-de-trabalho.md`. **Correções da auditoria de 2026-07-23**: `Layout.astro` ganhou `canonical`, JSON-LD (`ProfessionalService` em toda página + `FAQPage` extra na `/faq`, a partir do array `perguntas` que já existia), favicon PNG/apple-touch-icon (gerados do monograma via `scripts/gera-favicons.mjs`, mesmo padrão do `gera-og.mjs`) e o comentário pessoal que vazava pro HTML de produção foi removido; corrigido um contraste WCAG real no card "Não está incluído" de `Escopo.astro` (ficava sem fundo próprio, exposto ao trecho alaranjado do degradê); `BotaoWhatsApp.astro` do site passou a abrir em nova aba; nasceram `Secao.astro` e `BotaoContorno.astro` (extraídos da duplicação de 8 e 2 componentes, respectivamente) e uma `404.astro`; `package.json` ganhou script `check` (+ `overrides` do Vite do `@tailwindcss/vite`, mesmo fix que o `template/` já tinha) e moveu `sharp` para `dependencies`. **Domínio próprio conectado em 2026-07-27/28**: `codigoitinerante.com.br` (registrado no Registro.br em 2026-07-24) associado ao projeto Vercel `codigo-itinerante` via `vercel domains add` (já vinha associado ao projeto desde o registro; só faltava DNS). `www.codigoitinerante.com.br` definido como canônico: domínio raiz configurado com redirect 308 para o `www` via `vercel api -X PATCH /v9/projects/codigo-itinerante/domains/codigoitinerante.com.br -F redirect=www... -F redirectStatusCode=308` (campo `redirect` do domínio de projeto; não há flag disso em `vercel domains add`, é preciso a API direta). **DNS não foi delegado ao Vercel** — nameservers continuam os do Registro.br (`a.auto.dns.br`/`b.auto.dns.br`), decisão deliberada: manter o Registro.br como DNS provider e só apontar registros A é mais cirúrgico/reversível que trocar os nameservers inteiros. Falta a autora adicionar, em "Configurar endereçamento" no painel do Registro.br (não em "Alterar servidores DNS"), dois registros A: `@` → `76.76.21.21` e `www` → `76.76.21.21`; depois disso o Vercel verifica sozinho e emite o certificado. Vercel CLI instalado globalmente nesta máquina (estava ausente), login já ativo como `tatianaoburgos-1152` no time `codigo-itinerante`. **Nota de ambiente Windows**: no Git Bash, `vercel api /caminho` precisa do prefixo `MSYS_NO_PATHCONV=1` (sem isso o MSYS2 reescreve o path Unix-like como path do Windows e a chamada falha com "Invalid arguments", mesmo em exemplos da própria doc do comando). Pendente ainda: mostrar o Mar à Vista como segundo case em `ProvaDeTrabalho.astro` (avaliado e adiado por ora).

**Fase 5 do roteiro (risco comercial da permuta) concluída em 2026-07-23**: entrevista dirigida fechou os 8 eixos do modelo de permuta (contrapartida da estadia, preço/noites, rodadas de ajuste, processo/prazos, saída, manutenção, propriedade, formalização) — spec completa em `docs/superpowers/specs/2026-07-23-fase5-limites-permuta-design.md`. Valor de referência do site fixado em R$3.000 (parâmetro interno — nunca aparece no contrato do cliente nem no site público; só o número de noites resultante, calculado caso a caso no WhatsApp). Saiu da sessão o instrumento de aplicação (`docs/gera_questionario_contrato.py` → `docs/questionario-contrato.docx`, perguntas que mudam por contrato) e uma página nova no site da marca, `site/src/pages/faq.astro` — fora da rolagem única da home, alcançada por um link "Veja as perguntas frequentes" em `Escopo.astro` (visual: zona clara/"papel", como o `CtaFinal`, em vez do degradê da home, por ser página curta fora da narrativa calibrada por seção). Sete perguntas, cobrindo as 5 originais da Fase 5 mais duas que surgiram na sessão (material/fotos mínimas, propriedade de domínio/código). Corrigida junto uma linha desatualizada em `Escopo.astro` ("uma rodada de ajustes após a entrega" → "quatro rodadas... durante a construção") — a mesma correção foi replicada em `docs/projeto.md` §4/§5, que tinham a mesma linha obsoleta. **Texto real do contrato de permuta redigido e aprovado pela autora, mesmo dia**: `docs/contrato-permuta.md` (`docs/projeto.md` §12), modelo genérico em Markdown com 13 cláusulas — tom simples e direto, sem juridiquês, reaproveitando frases do FAQ. O `.docx` de cada contrato real (aplicando o visual da marca) é gerado sob demanda, por cliente, quando a autora entrega o questionário de intake preenchido — ver regra em "Convenções" acima.

**Fase 3 do roteiro concluída em 2026-07-19** (splash de cor + iconografia no `site/`): fundo da página em degradê contínuo Breu → Âmbar (classe `.pagina-degrade` em `site/src/styles/global.css`), breu sólido cobrindo Hero + QuemSouEu + ComoFunciona e a cor entrando a partir do Manifesto, chegando em âmbar cheio pouco antes do CTA final (paradas recalibradas em 2026-07-22 — ver seção de portfólio acima — para o breu passar a cobrir Hero + QuemSouEu + ProvaDeTrabalho e a cor entrar a partir do ComoFunciona). `CtaFinal` vive na zona clara (versão de papel: ícone/monograma claros, texto em Breu, botão invertido via nova prop `invertido` em `BotaoWhatsApp.astro`). Ícones de `marca/icones/` em escala grande no `ComoFunciona` (72px) e no `CtaFinal` (88px). `Hero` no mobile usa `min-h-dvh` (tela cheia) para a primeira dobra não revelar a seção seguinte — no desktop mantém `md:min-h-[78vh]`. Exceção à regra do acento registrada no `docs/miv.md`. Spec e plano: `docs/superpowers/specs/2026-07-19-site-degrade-fase3-design.md` e `docs/superpowers/plans/2026-07-19-site-degrade-fase3.md`. Ajuste fino em 2026-07-20 (feedback direto da autora): wordmark do Hero maior que o título (`w-[108%]`, título recuado para `text-4xl md:text-6xl`) para dar mais hierarquia ao nome da marca.

**Fase 2 do roteiro concluída em 2026-07-20** (template Word com a assinatura da marca): `marca/template-documento.docx` — cabeçalho/rodapé/tipografia conforme `docs/miv.md` §5, só o sistema visual (texto real de proposta/briefing/contrato fica para uma fase futura, a combinar com a autora). Spec `docs/superpowers/specs/2026-07-20-fase2-template-word-design.md`, plano `docs/superpowers/plans/2026-07-20-fase2-template-word.md` (5 tasks, todas completas; ledger em `.superpowers/sdd/progress.md`). Task 1 (commit `182fcf5`): `marca/prepara_fontes_documento.py` deriva faces estáticas "Archivo Doc"/"Archivo Doc Bold"/"Martian Mono Doc" das fontes variáveis (nomes próprios pra não colidir com uma instalação futura das fontes completas) e instala por usuário no Windows. Instrument Serif não entra nesta fase (o §5 do MIV não usa itálico serifado em documentos). Tasks 2-4 (commits `1277d02`, `c55fe67`, `1d44dc0`): `marca/gera_template_word.py` monta página/cabeçalho/rodapé, corpo com metadados/título/texto/destaque, e incorpora as fontes no `.docx` final via automação COM do Word. Task 5: documentação atualizada em `docs/miv.md` §7 e `docs/ideias-backlog.md`. Execução via subagent-driven-development.

**Fase 1 do MIV em andamento** (desde 2026-07-17): estilo de ilustração da marca e a personagem "viajante". Spec em `docs/superpowers/specs/2026-07-17-miv-ilustracao-viajante-design.md`, plano em `docs/superpowers/plans/2026-07-17-fase1-viajante-ilustracao.md` (checkboxes marcam o progresso). Fluxo: cada peça nasce como mockup HTML com variantes em `mockups/` (formato de comparação aprovado pela autora), passa por aprovação humana explícita e só então vira SVG definitivo em `marca/ilustracao/` — nenhuma task avança sem aprovação. Estado: Task 1 (silhuetas) pausada no portão de aprovação, com tendência parcial S2-P2 (`mockups/viajante-silhuetas.html`) que a autora considerou insuficiente. Em 2026-07-19 ela trouxe um desenho a lápis próprio (celular erguido, óculos escuros, cabelão cacheado, top cropped) e o vetorizou; o Claude coloriu por regiões em `mockups/viajante-colorida.svg` (fonte em `mockups/viajante-vetorizada-original.svg`) — aguardando a reação dela para promover a `marca/ilustracao/viajante.svg`. No mesmo dia ela pediu para deixar personagem/cidade/hero por último e seguir com o resto do roteiro. **Iconografia da marca concluída em 2026-07-19** (spec `docs/superpowers/specs/2026-07-19-iconografia-design.md`, plano `docs/superpowers/plans/2026-07-19-iconografia.md`): 6 conceitos do negócio em dois pesos e dois fundos, SVGs em `marca/icones/` gerados por `marca/gera_icones.py` (fonte de verdade do desenho: `mockups/icones-refino.html`), regras na seção 6 do `docs/miv.md`. Backlog de fases e ideias em `docs/ideias-backlog.md`.

Sitemap já integrado via `@astrojs/sitemap` em `template/astro.config.mjs` (+ `template/src/pages/robots.txt.ts`); a URL base ainda depende do domínio por cliente. Pendente: validação do LumeHostel pelo cliente (textos, fotos, contato real). As "variações 2 e 3" ficaram em aberto: a one-page substituiu a variação 1 para todos os clientes, então o que "variação" significa daqui pra frente precisa ser redefinido (provavelmente tema + composição, não estrutura de rotas).
