---
interface Props {
  texto: string;
  destaque?: string;
}

const { texto, destaque } = Astro.props;
---

<h2 class="font-mono text-2xl font-medium uppercase tracking-[0.12em] text-pedra">
  {texto}{destaque && <span class="text-ambar"> {destaque}</span>}
</h2>
