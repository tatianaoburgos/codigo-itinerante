---
interface Props {
  texto: string;
  destaque?: string;
}

const { texto, destaque } = Astro.props;
---

<h2 class="font-mono text-xs font-medium uppercase tracking-[0.12em] text-pedra">
  {texto}{destaque && <span class="text-ambar"> {destaque}</span>}
</h2>
