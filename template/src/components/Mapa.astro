---
import { config } from '../lib/cliente';
---

<iframe
  src={config.localizacao.mapsEmbedUrl}
  title={`Mapa: ${config.localizacao.endereco}`}
  loading="lazy"
  referrerpolicy="no-referrer-when-downgrade"
  class="aspect-video w-full rounded-xl border-0"></iframe>
